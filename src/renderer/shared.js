export default {
  Lang: function(short_name, full_name, gpc_name, selected){
    this.short_name = short_name
    this.full_name = full_name
    this.gpc_name = gpc_name
    this.selected = selected
  }
}